#pragma once

#include <array>

// define types
typedef int node_idx_t;       // type to hold value of node index
typedef node_idx_t element_idx_t;   // type to hold value of element index
typedef std::array<double, 2> Vec2;    // 2D vector
typedef std::array<double, 3> Vec3;    // 3D vector to store position of a node