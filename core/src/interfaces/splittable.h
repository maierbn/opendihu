#pragma once

#include <Python.h>  // has to be the first included header
#include <iostream>

/**
 *  Class that can be part of a splitting scheme
 */
class Splittable
{
public:
  //! constructor
  Splittable();
};
