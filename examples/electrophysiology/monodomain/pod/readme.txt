For running the reduced-order scheme, one should first run the original (full-order) scheme to gather snapschots. Copy the snapshots from the out to out_snapshots folder. Run the snapshot.py script on the snapschots to prepare the csv data, which is needed to run the reduced order method.
-Nehzat
